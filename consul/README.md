`registration.js` and `package.json` came from [OpenLMIS](https://github.com/OpenLMIS/openlmis-template-service)

`registration.js` was modified to allow container ip address to be specified as an argument.